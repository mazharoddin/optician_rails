<% @title = "Show Lens Type" %>
<div class="page-header">
	<h1><%= @lens_type.name %> (Lens Type)<% if can? :edit, @lens_type %><%= link_to "Edit Lens Type", edit_admin_lens_type_path(@lens_type), :class => "btn btn-primary pull-right" %><% end %></h1>
</div>


<dl>
	<dt>Name</dt>
	<dd><%= @lens_type.name %></dd>
	<dt>Active</dt>
	<dd><%= @lens_type.active %></dd>
</dl>
