<% @title = "Show Tax" %>
<div class="page-header">
	<h1><%= @tax.name %> (Tax)<% if can? :update, @tax %><%= link_to "Edit Tax", edit_admin_tax_path(@tax), :class => "btn btn-primary pull-right" %><% end %></h1>
</div>


<dl>
	<dt>Name</dt>
	<dd><%= @tax.name %></dd>
	<dt>Rate</dt>
	<dd><%= @tax.rate %></dd>
</dl>
