<%= simple_form_for [:admin, @employment_type], :html => { :class => "form-horizontal" } do |f| %>
	<%= f.input :name %>
	<%= f.input :active %>

	<div class="form-actions">
		<%= f.button :submit, :class => "btn btn-primary" %>
<% if @employment_type.new_record? %>
		<%= link_to "Discard Changes", admin_employment_types_path, :class => "btn" %>
<% else %>
		<%= link_to "Discard Changes", admin_employment_type_path(@employment_type), :class => "btn" %>
<% end %>
	</div>
<% end %>