<% @title = "Show Company" %>
<div class="page-header">
	<h1><%= @company.name %> (Company)<% if can? :update, @company %><%= link_to "Edit Company", edit_admin_company_path(@current_account, @company), :class => "btn btn-primary pull-right" %><% end %></h1>
</div>


<dl>
	<dt>Name</dt>
	<dd><%= @company.name %></dd>
	<dt>Active</dt>
	<dd><%= @company.active %></dd>
</dl>
