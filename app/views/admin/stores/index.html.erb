<% @title = "Stores" %>
<div class="page-header">
	<h1>Find Store <%= link_to "New Store", new_admin_store_path, :class => "btn btn-success pull-right" %></h1>
</div>
<form class="well form-search">
	<input type="text" class="search-query">
	<button type"submit" class="btn btn-info">Search</button>
</form>

<% if @stores.empty? then %>
<p>No stores found</p>
<% else %>
<table class="table table-condensed table-striped">
	<thead>
		<tr>
			<th>Name</th>
			<th>Address</th>
			<th>City</th>
			<th>Postal Code</th>
			<th>State</th>
			<th>Phone</th>
		</tr>
	</thead>
	<tbody>
<% @stores.each do |store| %>
		<tr>
			<td><%= link_to store.name, admin_store_path(store) %></td>
			<td><%= store.address %></td>
			<td><%= store.city %></td>
			<td><%= store.postal_code %></td>
			<td><%= store.state %></td>
			<td><%= store.phone %></td>
		</tr>
<% end %>
	</tbody>
</table>
<%= paginate @stores %>
<% end %>