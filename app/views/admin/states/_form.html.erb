<%= simple_form_for [:admin, @country, @state], :html => { :class => "form-horizontal" } do |f| %>
	<%= f.input :name %>
	<%= f.input :short_name %>
	<%= f.button :submit, :class => "btn btn-primary" %>
<% end %>