		<div class="navbar navbar-fixed-top navbar-inverse">
			<div class="navbar-inner">
				<div class="container">
					<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
					</a>

					<a class="brand" href="<%= @current_account == nil ? root_path : dashboard_path(@current_account) %>" style="font-size: 21px; font-weight: bold; font-family: Calibri;"><span style="color: #b5e61d">optician</span><span style="color: #ffffff">plus</span></a>
<% if current_user != nil && @current_account != nil %>
					<div class="nav-collapse">
						<ul class="nav">
							<li <%= raw (@navbar_selected == 'dashboard') ? 'class="active"' : '' %>><%= link_to "Dashboard", dashboard_path(@current_account) %></li>
							<% if can? :read, Invoice %><li <%= raw (@navbar_selected == 'invoices') ? 'class="active"' : '' %>><%= link_to "Invoices", invoices_path %></li><% end %>
							<% if can? :read, Patient %><li <%= raw (@navbar_selected == 'patients') ? 'class="active"' : '' %>><%= link_to "Patients", patients_path %></li><% end %>
							<% if can? :read, Inventory %><li <%= raw (@navbar_selected == 'inventory') ? 'class="active"' : '' %>><%= link_to "Inventory", inventory_root_path %></li><% end %>
							<% if current_user.administrator || current_user.super_administrator %><li <%= raw (@navbar_selected == 'administration') ? 'class="active"' : '' %>><%= link_to "Administration", admin_root_path %></li><% end %>
						</ul>

						<ul class="nav pull-right">
							<li class="dropdown">
								<a href="#" class="dropdown-toggle" data-toggle="dropdown" >Welcome, <% if user_signed_in? %><%= current_user.first_name %><% else %>Not Logged In<% end %><b class="caret"></b></a>
								<ul class="dropdown-menu">
									<li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
								</ul>
							</li>
						</ul>
					</div>
<% end %>
				</div>
			</div>
		</div>
